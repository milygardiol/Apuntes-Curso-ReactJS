//  # Componentes 2
// Anatomía de un componente:
// 1. Props (Parámetros que se le pasa a los componentes)
// 2. State(Persistir información del ordenador)
// 3. Lifecycle(Estados de vida el componente)
// 4. DOM(Javascript)

// Render props (escribir)

// Class based components
// this.setState (setear) genera un nuevo render 
// Persisten datos que se emplean para que al app se renderice

// Funcion based (escribir)

// *State hook*
// - El estado es el principal mecanismo para alterar la interfaz
// - La app se aplica de una forma, pero no se altera la interfaz, lo que hace el estado al cambiar, genera un nuevo render del componente.
// - No se colocan en las estructuras (IF, FOR, TERNARIOS)
// - Se ejecutan en orden

// Efectos/Cheatsheet
// useEffect => (Escribir) 
